# --- Backend lives in its own repo (valheim-donations-backend) ---
backend/

# --- Copyrighted Valheim / Unity assemblies — never redistribute ---
valheim-plugin/libs/
*.dll

# --- Thunderstore packaging output (zips are built on demand, per release) ---
# The package METADATA under Thunderstore files/<pkg>/ is tracked; only the
# built archives are not. Matches the sibling Lost Scrolls II repo.
Thunderstore files/*.zip

# --- .NET build output ---
valheim-plugin/bin/
valheim-plugin/obj/
[Bb]in/
[Oo]bj/

# --- Python (setup-guide generator) ---
__pycache__/
*.pyc
.venv/

# --- Local tooling / editor ---
.claude/
.vs/
.idea/
.DS_Store
Thumbs.db
